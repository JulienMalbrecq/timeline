export default class TimeLineTool {
    constructor (name) {
        this.name = name;
        this.active = false;
    }

    mouseDown (line, tile) {}
    mouseMove (line, tile) {}
    mouseUp   (line, tile) {}
}